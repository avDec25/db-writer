db:
  couchbase-writes: false
  oracle-writes: true
  reads: oracle

spring:
  application:
    name: db-writer

  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: system
    password: Oracle123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true